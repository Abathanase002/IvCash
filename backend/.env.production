# IvCash Production Environment Variables
# ========================================
# Copy these to Railway's environment variables

# Application
NODE_ENV=production
PORT=3000
API_PREFIX=api/v1

# Database - PostgreSQL (Railway provides these automatically)
# DATABASE_URL will be auto-injected by Railway
# Or set individual vars:
# DB_HOST=${{Postgres.PGHOST}}
# DB_PORT=${{Postgres.PGPORT}}
# DB_USERNAME=${{Postgres.PGUSER}}
# DB_PASSWORD=${{Postgres.PGPASSWORD}}
# DB_DATABASE=${{Postgres.PGDATABASE}}

# JWT Configuration (CHANGE THESE!)
JWT_SECRET=your-super-secure-production-jwt-secret-change-this
JWT_EXPIRES_IN=7d

# Redis (Railway provides these automatically)
# REDIS_URL will be auto-injected by Railway

# CORS - Update with your actual domain
FRONTEND_URL=https://ivcash-admin.up.railway.app
ADMIN_URL=https://ivcash-admin.up.railway.app

# Email Configuration (Optional - for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
EMAIL_FROM=noreply@ivcash.com

# SMS Configuration (Optional)
SMS_API_KEY=your-sms-api-key
SMS_SENDER_ID=IvCash
